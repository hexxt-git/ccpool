# Copy to apps/server/.env for local dev (the dev script reads it via --env-file).
# `make db-up` starts a throwaway Postgres on 5433 with these credentials.
DATABASE_URL=postgres://ccshare:ccshare@localhost:5433/ccshare
# PORT=8787
