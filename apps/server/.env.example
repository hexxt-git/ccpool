# Copy to apps/server/.env for local dev (the dev script reads it via --env-file).
# libSQL: a local SQLite file for dev, or a remote libsql://… (Turso) URL.
DATABASE_URL=file:./ccshare-dev.db
# For a remote Turso database, also set:
# CCSHARE_DB_AUTH_TOKEN=…
# PORT=8787
