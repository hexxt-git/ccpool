{
  "name": "@ccpool/core",
  "type": "module",
  "version": "0.0.1",
  "description": "Runtime-agnostic domain logic for ccpool: the storage boundary, ingest/view backend pieces, attribution, view assembly, and the wire contract.",
  "license": "MIT",
  "private": true,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.2"
  }
}
